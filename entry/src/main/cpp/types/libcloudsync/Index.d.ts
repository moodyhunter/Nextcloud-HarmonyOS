export const add: (alias: string, path: string) => number;

export const remove: (path: string) => number;

export const connect: (path: string) => number;

export const disconnect: (path: string) => number;
