export enum FolderViewMode {
    OneColumn = 1,
    TwoColumns = 2,
    Grid = 3
}

export namespace Constants {
    export const VIEW_MODE_KEY = 'FolderList.FolderViewMode';

    export const PC_CLOUD_DISK_PATH = 'PC.CloudDisk.Path';
}
